<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.zerok.mapper.ex03.Ex03Mapper">
  <select id="selectCustomerNameById" resultType="string">
    SELECT CustomerName FROM Customers WHERE CustomerID = #{id}
  </select>
  
  <select id="selectEmployeeFirstNameById" resultType="string">
  	SELECT FirstName 
  	FROM Employees 
  	WHERE EmployeeID = #{id}
  </select>
  
  <select id="selectBoard" resultType="org.zerok.domain.ex02.BoardDto">
  	SELECT b.id, b.title, b.inserted, COUNT(r.id) numOfReply
  	FROM Board b LEFT JOIN Reply r ON b.id = r.board_id
  	GROUP BY b.id
  	ORDER BY id DESC 
  </select>
  
  <select id="getBoard" resultType="org.zerok.domain.ex02.BoardDto">
  	SELECT id, title, body, inserted 
  	FROM Board 
  	WHERE id = ${id}
  </select>
  
  <update id="updateBoard">
  	UPDATE Board
  	SET title = #{title}, 
  		body = #{body}
  	WHERE id = #{id}
  </update>
  
  <delete id="deleteBoard">
  	DELETE FROM Board
  	WHERE id = #{id}
  </delete>
  <!-- keyColumn = id => db에 있는 id keyProperty = id => javaBeans-->
  <insert id="insertBoard" useGeneratedKeys="true" keyColumn="id" keyProperty="id">
  	INSERT INTO Board (title, body, inserted) 
  	VALUES (#{title}, #{body}, #{inserted})
  </insert>
  
  <delete id="deleteReplyByBoard">
  	DELETE FROM Reply
  	WHERE board_id = #{boardId }
  </delete>
  
  <!-- 
  insert
  update
  delete
   -->
</mapper>
